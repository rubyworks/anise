# Load domain components.
#require 'reap/domain/script'
#require 'reap/domain/rule'

# this is callable from the commandline 
#annotation :cmd

# command line options
#annotation :opt

# define a description (from help and list)
#annotation :desc

#def commands
#  c = {}
#  annotations.each do |n, a|
#    if a.key?(:cmd)
#      c[n] = a[:cmd]
#    end
#  end
#  c
#end

#def import(name)
#  script = LIB_DIRECTORY / 'systems' / (name + '.reap')
#  instance_eval(File.read(script))
#end

